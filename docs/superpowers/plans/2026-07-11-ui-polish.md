# UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all audited responsive, navigation, content, and map presentation defects while preserving the current design.

**Architecture:** Keep fixes local to the existing React components, CSS modules, and shared content source. Use Playwright geometry assertions for visual contracts and a small post-render hash effect for SPA navigation.

**Tech Stack:** React 19, TypeScript, CSS modules, Vitest, Testing Library, Playwright.

## Global Constraints

- Do not invent hotel information or change registry destinations.
- Preserve desktop styling and existing security behavior.
- Keep required OpenStreetMap attribution visible.
- Preserve unrelated generated responsive-image files.

---

### Task 1: Add failing regression coverage

**Files:**
- Modify: `apps/web/e2e/home.spec.ts`
- Modify: `apps/web/src/App.test.tsx`
- Modify: `packages/shared/src/index.test.ts`

- [ ] Add tests asserting a compact one-row mobile nav, non-overlapping admin panels, RSVP title/form/steps order, post-render Details scrolling, marker-free map configuration, and removal of audited placeholder strings.
- [ ] Run `npm test -- packages/shared/src/index.test.ts apps/web/src/App.test.tsx` and the focused Playwright tests; confirm each new assertion fails for the audited reason.

### Task 2: Implement responsive component fixes

**Files:**
- Modify: `apps/web/src/components/SiteLayout.module.css`
- Modify: `apps/web/src/pages/AdminPage.module.css`
- Modify: `apps/web/src/pages/RsvpPages.tsx`
- Modify: `apps/web/src/pages/RsvpPages.module.css`

- [ ] Make the 390px header a stable two-row layout with all primary links on one row.
- [ ] Override the admin login grid locally to one column at the responsive breakpoint.
- [ ] Separate the RSVP intro from its steps and order intro, form, then steps on mobile.
- [ ] Rerun the focused Playwright geometry tests until green.

### Task 3: Fix hash navigation, content, and map presentation

**Files:**
- Modify: `apps/web/src/App.tsx`
- Modify: `apps/web/src/pages/PublicPages.tsx`
- Modify: `apps/web/src/pages/PublicPages.module.css`
- Modify: `packages/shared/src/siteContent.ts`
- Modify: `docs/ARCHITECTURE.md`

- [ ] Add a guarded post-render hash scroll and responsive anchor margin.
- [ ] Hide unconfirmed hotels, replace placeholder copy/alts, and align registry copy with live links.
- [ ] Remove the iframe marker query, add an accessible app-owned marker, and increase mobile map height without hiding attribution.
- [ ] Rerun focused unit and Playwright tests until green.

### Task 4: Verify and review

- [ ] Run `npm run typecheck`, `npm test`, `npm run lint`, and `npm run build -w apps/web`.
- [ ] Run `npm run test:e2e` with a fresh `E2E_PORT` and confirm the port closes afterward.
- [ ] Inspect `/`, `/our-story`, `/registry`, `/rsvp`, and `/admin` at desktop and 390px mobile widths in the in-app browser, including the Details interaction and console health.
- [ ] Obtain independent reviewer approval before creating the pull request.
