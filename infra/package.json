{
  "name": "@matt-alison-wedding/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap": "cdk bootstrap",
    "build": "tsc -p tsconfig.json",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "synth": "cdk synth",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.266.0",
    "constructs": "^10.8.1",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "aws-cdk": "^2.1138.0",
    "esbuild": "^0.28.2",
    "ts-node": "^10.9.2"
  }
}
